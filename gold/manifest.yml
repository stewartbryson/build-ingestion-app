manifest_version: 1 # required
version:
  name: brokerage_gold
  label: "v1.0"
  comment: "Our gold layer."

artifacts:
  readme: README.md
  setup_script: scripts/setup.sql
  #default_streamlit: streamlit/ui.exchange_analytics
  extension_code: true

configuration:
  log_level: debug
  trace_level: always

